services:
  warp:
    build:
      target: dev # Only for devcontainer
    environment:
      - RUN_WARP=false # We don't need these in the devcontainer
      - RUN_PLASMA=false
    volumes:
      - .:/workspace/warp # Mount our workspace folder