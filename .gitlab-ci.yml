stages:
  - build

variables:
  DOCKER_DRIVER: overlay2
  DOCKER_TLS_CERTDIR: "/certs"

build-docker:
  stage: build
  script:
    - echo "Building Warp Docker container..."
    - docker build -t warp:test -f Dockerfile .
    - echo "Docker build completed successfully"
    - docker images | grep warp
  rules:
    - if: $CI_COMMIT_BRANCH